server:
  port: 9004
spring:
  application:
    name: livegoods-details
#  profiles:
#    active: mongodb,bannerNginx,redis
  cloud:
    config:
      uri: http://localhost:9010
      label: main
      name: livegoods
      profile: dev